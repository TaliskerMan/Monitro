# Monitro Security Scan Results

**Application:** Monitro v1.2.13  
**Date:** April 21, 2026  
**Scanner:** Snyk (via Snyk MCP Server)  
**Scanned By:** Antigravity AI  
**Repository:** `/home/freecode/antigrav/Monitro`

---

## Overview

A comprehensive security audit was performed across the full Monitro codebase, covering both the Flutter UI frontend and the Dart backend collector service. The audit addressed two primary security domains:

| Domain | Description |
|---|---|
| **SAST** (Static Application Security Testing) | Analyses first-party Dart/Flutter source code for coding vulnerabilities such as injection flaws, insecure cryptography, path traversal, hardcoded secrets, and similar weaknesses. |
| **SCA** (Software Composition Analysis) | Analyses `pubspec.yaml` / `pubspec.lock` and all resolved dependency trees for known CVEs and vulnerabilities in third-party open-source packages. |

---

## Scan Results

### Full Project (`/Monitro`)

| Scan Type | Tool | Result |
|---|---|---|
| SCA — Dependency Vulnerabilities | `snyk test --all-projects` | ✅ **0 issues found** |
| SAST — First-Party Code | `snyk code test` | ✅ **0 issues found** |

### Backend Sub-Project (`/Monitro/backend`)

| Scan Type | Tool | Result |
|---|---|---|
| SCA — Dependency Vulnerabilities | `snyk test` | ℹ️ Covered by root-level scan (no separate manifest needed) |
| SAST — First-Party Code | `snyk code test` | ✅ **0 issues found** |

---

## Dependency Inventory

The following production dependencies were assessed as part of the SCA scan:

| Package | Version Constraint | Category |
|---|---|---|
| `http` | ^1.2.0 | HTTP & REST client |
| `dio` | ^5.4.0 | HTTP & REST client |
| `fl_chart` | ^0.68.0 | Data visualisation |
| `flutter_riverpod` | ^2.4.0 | State management |
| `riverpod_annotation` | ^2.3.0 | State management |
| `yaml` | ^3.1.2 | Configuration parsing |
| `go_router` | ^13.0.0 | Routing |
| `intl` | ^0.19.0 | UI utilities |
| `timeago` | ^3.6.1 | UI utilities |
| `shimmer` | ^3.0.0 | UI utilities |
| `lottie` | ^3.0.0 | UI utilities |
| `shared_preferences` | ^2.2.2 | Local persistence |
| `cupertino_icons` | ^1.0.6 | Icons |
| `material_symbols_icons` | ^4.2719.3 | Icons |
| `system_info2` | ^4.0.0 | System information |
| `path_provider` | ^2.1.5 | Platform paths |
| `path` | ^1.9.1 | Path utilities |
| `package_info_plus` | ^9.0.0 | Package metadata |

**No known CVEs or vulnerability advisories were found for any of the above packages.**

---

## Conclusion

The Snyk security suite found **no known vulnerabilities** in either the Monitro first-party code or its third-party open-source dependencies. The application is currently secure and hardened against:

- Known dependency exploits (CVEs)
- Injection flaws (SQL, command, path traversal)
- Insecure cryptographic patterns
- Hardcoded credentials or secrets
- Insecure network communication patterns
- Other OWASP Top 10 class vulnerabilities detectable via static analysis

---

## Recommendations

> [!NOTE]  
> This report reflects a point-in-time assessment. Security is an ongoing practice.

1. **Re-run scans periodically** — New CVEs are published daily. Schedule monthly SCA sweeps at minimum.
2. **Re-run after every dependency update** — Any change to `pubspec.yaml` should trigger a fresh `snyk test`.
3. **Re-run SAST after significant feature additions** — New code paths introduce new risk surfaces.
4. **Monitor Snyk advisories** for packages in the inventory above, particularly `dio` and `http` which handle external network communication.

---

*Report generated by Antigravity AI · Nordheim Online · April 21, 2026*

---

## Scope note (what this scan does and does NOT cover)

This Snyk SAST/SCA pass checks dependency CVEs and common code-level sink
patterns. It does **not** validate Monitro's actual risk surface, which is
architectural:

- **Local API exposure.** A clean SAST result says nothing about
  `Access-Control-Allow-Origin: *` combined with an optional API key exposing
  `DELETE /processes/<pid>` to any local web origin. (Fixed in 1.2.14: the API
  now refuses to start without an `api_key` and no longer emits a permissive
  CORS header.)
- **Fail-open UI behaviour.** SAST cannot see that a `403`/`500` rendered every
  dashboard card blank with no visible error. (Fixed in 1.2.14: backend errors
  are surfaced in a banner and a collector status indicator.)
- **TLS pinning.** A green scan does not verify that the client validates the
  generated CA rather than trusting any localhost certificate. (Fixed in
  1.2.14: the client pins the Monitro CA when present.)

Treat a clean Snyk result as "no known-vulnerable dependencies," not as "safe to
sign."
